name: Upload Website

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "14"

      - name: Install dependencies
        run: npm install

      - name: Build app
        run: npm run build

      - name: Upload to S3
        uses: jakejarvis/s3-sync-action@master
        with:
          args: --acl public-read
        env:
          SOURCE_DIR: build/
          SANKALP_AWS_S3_BUCKET: ${{ secrets.SANKALP_AWS_S3_BUCKET }}
          SANKALP_AWS_KEY_ID: ${{ secrets.SANKALP_AWS_KEY_ID }}
          SANKALP_AWS_SECRET_KEY_ID: ${{ secrets.SANKALP_AWS_SECRET_KEY_ID }}
